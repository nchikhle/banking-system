spring:
  application:
    name: audit-service

  datasource:
    url: jdbc:h2:mem:audit_db
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

  h2:
    console:
      enabled: true

server:
  port: 8084